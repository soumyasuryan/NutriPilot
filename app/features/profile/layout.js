export const metadata = {
  title: 'Profile & Progress | NutriPilot',
  description: 'View your caloric history and personal food library.',
}

export default function Layout({ children }) {
  return children;
}
