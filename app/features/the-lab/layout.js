export const metadata = {
  title: 'The Lab | NutriPilot',
  description: 'Set your nutrition goals and get baseline diagnostics.',
}

export default function Layout({ children }) {
  return children;
}
