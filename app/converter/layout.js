export const metadata = {
  title: 'Kitchen Converter | NutriPilot',
  description: 'AI-powered rough natural language measurements converter.',
}

export default function Layout({ children }) {
  return children;
}
